This is an experimental vulkan renderer written in rust using the vulkano crate.

It currently only rasterizes static 3D GLTF/GLB files, but I would eventually like to use it to experiment with ray tracing and optimization techniques.