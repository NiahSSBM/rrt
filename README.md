This is an experimental vulkan renderer written in rust using the vulkano crate.

It currently only rasterizes 3D STL models, but I would eventually like to use it to experiment with ray tracing and optimization techniques.